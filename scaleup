trigger: none

schedules:
  - cron: "30 15 * * 1-5"
    displayName: 'Daily 9 PM ist Schedule'
    branches:
      include:
        - develop
    always: true

jobs:
  - job: ScaleDown
    displayName: ScaleDown of postgresql servers
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'acrServiceConnectionLowerSubscription'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
              az postgres flexible-server update --sku-name Standard_D2ds_v5 --resource-group "to-devnew" --name "mo-dev-flexiserver-postgresql" --yes
